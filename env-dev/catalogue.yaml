#env:
#  MONGO: true
#  MONGO_URL: "mongodb://mongodb-dev.tejadevopsb81.icu:27017/catalogue"

appName: catalogue
service:
  containerPort: 8080
  type: ClusterIP
  servicePort: 8080

schema:
  enabled: true
  dbExtension: js
  containers:
    - master-data