#env:
#  CART_ENDPOINT: "cart:8080"
#  DB_HOST: mysql-dev.tejadevopsb81.icu

appName: shipping
service:
  containerPort: 8080
  type: ClusterIP
  servicePort: 8080

schema:
  enabled: true
  dbExtension: sql
  containers:
    - app-user
    - schema
    - master-data